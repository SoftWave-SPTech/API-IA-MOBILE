spring:
  datasource:
    url: jdbc:h2:mem:finance_ia_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false

features:
  usar-metricas-mock: true
  fallback-metricas-mock-em-falha: true
